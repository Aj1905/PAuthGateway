"""Task suites for the PAuth reproduction.

A *suite* exposes tools, an environment factory and a set of tasks in a
framework-neutral form (:mod:`pauth.suites.base`).  The shopping and dining
suites are self-contained; external benchmarks (the four AgentDojo suites,
and future ones) are adapted under :mod:`benchmarks`.
"""
