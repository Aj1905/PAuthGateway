"""Task suites for the PAuth reproduction.

A *suite* exposes tools, an environment factory and a set of tasks in a
framework-neutral form (:mod:`pauth.suites.base`).  The shopping suite is
self-contained; the four standard AgentDojo suites are adapted in
:mod:`experiment.agentdojo_adapter`.
"""
