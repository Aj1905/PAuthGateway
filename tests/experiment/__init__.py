"""Experiment data artifacts: the AgentDojo A1 ``cache/`` and ``results/``
(both git-ignored), referenced by ``eval.fpfn`` (``CACHE_DIR`` / ``RESULTS_DIR``).

The benchmark *adapters* now live in the top-level :mod:`benchmarks` package;
only the generated data remains here.
"""
