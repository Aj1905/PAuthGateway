"""Live-deployment verification: score a real agent's run through the gateway.

Unlike ``eval/`` (deterministic, offline, no agent), this package is run by an
operator against their own deployed gateway + agent. See real_eval/README.md.
"""
