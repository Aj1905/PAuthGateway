# Copy this file to `.env` and fill in your key to run the A1 (code-generation)
# step on the AgentDojo suites.  The shopping suite and the offline tests do
# NOT need a key.

OPENAI_API_KEY=sk-your-key-here

# Model used for A1 imperative-code generation (paper uses gpt-4.1).
PAUTH_MODEL=gpt-4.1
